/** Canonical site origin — keep sitemap, metadata, and JSON-LD in sync. */
export const SITE_URL = "https://jaquevan.com" as const;

export const defaultOgImagePath = "/snare_close.png" as const;
